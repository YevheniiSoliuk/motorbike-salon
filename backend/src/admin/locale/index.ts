import { en } from './en';
import { pl } from './pl';

export const locale = {
  language: 'en',
  availableLanguages: ['en', 'pl'],
  localeDetection: true,
  translations: {
    pl: pl,
    en: en,
  },
};
