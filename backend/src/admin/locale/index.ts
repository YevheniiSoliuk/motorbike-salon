import { en } from './en';
import { pl } from './pl';

export const locale = {
  language: 'en',
  availableLanguages: ['en'],
  localeDetection: true,
  translations: {
    pl: pl,
    en: en,
  },
};
